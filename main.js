const product = 'Socks'

const app = Vue.createApp({
    data(){
        return {
            product: 'Socks',
            description: 'These socks are comfortable and provide nice value'
        }
    }
})